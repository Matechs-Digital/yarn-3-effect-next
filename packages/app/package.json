{
  "name": "app",
  "scripts": {
    "clean": "yarn g:rimraf build .next pages",
    "dev": "yarn clean && yarn g:ttsc --project tsconfig.build.json && yarn g:concurrently \"yarn g:ttsc --project tsconfig.build.json --watch\" \"yarn g:next dev\""
  }
}

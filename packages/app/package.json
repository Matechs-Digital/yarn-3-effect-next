{
  "name": "@org/app",
  "scripts": {
    "clean": "yarn g:rimraf lib .next pages .tsbuildinfo",
    "dev": "yarn build-src && yarn build-pages && yarn g:concurrently \"yarn build-src --watch\" \"yarn build-pages --watch\" \"yarn g:next dev\"",
    "build-src": "yarn g:ttsc --project tsconfig.build-src.json",
    "build-pages": "yarn g:ttsc --project tsconfig.build-pages.json",
    "build-next": "yarn g:next build",
    "build": "yarn clean && yarn g:ttsc --project tsconfig.build.json && yarn build-next"
  },
  "exports": {
    "./*": {
      "default": "./lib/*"
    }
  }
}
